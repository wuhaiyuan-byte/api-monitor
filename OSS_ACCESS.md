# OSS 文件监控 — 访问地址与凭据

## API 基址

```
http://192.168.3.219:9876
```

## 凭据

无认证（内网服务，暂无鉴权）。直接调用即可。

## 注意事项

- `9876` 端口是该服务的 host 端映射（容器内仍为 8000）。原 8000 在当前网络被过滤，9876 是已验证可通的端口。
- 服务在 `192.168.3.219` 内网，仅限同网段访问。
- 所有时间戳字段（`checked_at` / `last_modified` / ISO 字符串）均为 **UTC**。北京时 = UTC + 8。

## API 文档

详细的字段定义、状态机、Python/TS 示例见：`OSS_API_DOC.md`（同目录）。